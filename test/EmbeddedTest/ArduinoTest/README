This copies the contents of the source code into a C++ file, and compiles it as an executable to be run with the custom test-framework. To run tests, just run RunTests.sh or RunTests.bat from the terminal.